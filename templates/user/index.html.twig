{% extends 'base.html.twig' %}

{% block title %}Users
{% endblock %}

{% block body %}
{% include 'home/nav.html.twig' %}
	{# <style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style> #}

	<div class="example-wrapper">
		{# <h1>Hello
			{% if app.user  %}

				{{ app.User.lastname }}
				
			{% endif %}
		</h1> #}

	</div>
<section class="container mt-5">
		<div class="row">
			<div class="col">
    <table class="table table-hover">
	{% for message in app.flashes('message') %}
		<div class="alert alert-success " role="alert">
			
			{{ message }}
			<i data-bs-dismiss="alert" aria-label="Close" class="bi bi-x-lg pl-5"></i>
		</div>
	{% endfor %}
  <thead>
    <tr>
      <th scope="col">Last Name</th>
      <th scope="col">Frist Names</th>
      <th scope="col"> Email</th>
      
    </tr>
  </thead>
  <tbody>
   
   {% for user  in users %}
    <tr class="table-secondary">
      <th scope="row">{{user.lastname}}</th>
      <td>{{user.firstname}}</td>
      <td>{{user.email}}</td>
	  <td><a href="user/edit/{{user.id}}" class="text-success">Update</a></td>
	  <td><a href="user/delete/{{user.id}}">Delete</a></td>
    </tr>
   {% endfor %}
    
  </tbody>
</table>
</div>
</div>
</section>
{% endblock %}
