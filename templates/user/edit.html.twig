{% extends 'base.html.twig' %}

{% block title %}Users
{% endblock %}

{% block body %}
{% include 'home/nav.html.twig' %}
<section class="container my-5 ">
		<div class="row">
			<div class="col">
			{% for message in app.flashes('message') %}
		<div class="alert alert-danger " role="alert">
			
			{{ message }}
			<i data-bs-dismiss="alert" aria-label="Close" class="bi bi-x-lg pl-5"></i>
		</div>
	{% endfor %}
{{ form_start(form) }}

{{ form_widget(form) }}
<br>
<button type="submit" class="btn btn-success">update</button>
<a href="/user/edit_Password/{{user.id}}" class="text-success">Update password</a>
{{ form_end(form) }}
</div>
</div>
</section>
{% endblock %}
